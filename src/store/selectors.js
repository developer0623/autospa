import * as Alert from './Alert/selectors'
import * as User from './User/selectors'
import * as Service from './Service/selectors'
import * as Location from './Location/selectors'
import * as Booking from './Booking/selectors'

export const AlertSelectors = { ...Alert }
export const UserSelectors = { ...User }
export const ServiceSelectors = { ...Service }
export const LocationSelectors = { ...Location }
export const BookingSelectors = { ...Booking }
